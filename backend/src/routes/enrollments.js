const express = require('express');
const router = express.Router();

// Placeholder routes - to be implemented
router.get('/', (req, res) => res.json({ message: 'Enrollments API - Coming soon' }));
router.post('/', (req, res) => res.json({ message: 'Create enrollment - Coming soon' }));

module.exports = router;